import styled from 'styled-components';

export const SearchTemplateWrapper = styled.main`
	padding: 20px 40px;

	h1 {
		text-transform: capitalize;
		font-size: 40px;
	}
`;

export const NoPhotosText = styled.p`
	font-size: 36px;
	width: 100%;
	margin-top: 60px;
	text-align: center;
	color: black;
`;
